class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
       unordered_set<int> seen(nums.begin(), nums.end());
       int n = nums.size();
       if(n!=seen.size()) return true;
       return false;
    }
};

TC - O(n)
SC - O(n)
